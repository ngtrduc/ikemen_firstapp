<% if user_signed_in? %>
<div id="follow_form">
	<% if current_user.liking?(image) %>
		<%= render 'like', image: image %>
	<% else %>
		<%= render 'unlike', image: image %>
	<% end %>
</div>
<% end %>