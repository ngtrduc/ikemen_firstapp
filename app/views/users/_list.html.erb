  <% @users.each do |user| %>
  <div class="col-sm-5 well dobong vitri">
    <h2>
		  <%= link_to (image_tag(user.avatar.url(:medium), class: "img-circle",height: "50", width: "50")),"/user/#{user.id}"  %>
		  <%= link_to user.email, "/user/#{user.id}" %>

	</h2>
	<p> <%= link_to "#{user.image.count} pictures", "/user/#{user.id}", class: "vitri" %></p>
  </div>
  <%end%>